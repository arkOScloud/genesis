<div class="ui-firstrun-container">
  <div class="text-center">
    <h3 class="page-header">Step 5: Configuring arkOS</h3>
    <p>The following actions are underway. Please do not turn off power to your server or close this browser window.</p>
    <div class="well">
      <p class="lead">Your recovery password is:</p>
      <h1 style="font-family: monospace">{{rootPwd}}</h1>
      <p>Please make note of this password and keep it in a safe place. Use it for administrator (root) access in case it becomes necessary.</p>
    </div>
  </div>
  <table class="table">
    <tr header="True">
    	<th width="1" />
      <th>Operation</th>
      <th>Item</th>
    </tr>
    <tr class="{{if operations.user 'success'}}">
      <td><i class="fa fa-fw {{if operations.user 'fa-check' 'fa-spinner'}} {{if operations.user 'text-success' 'fa-spin'}}"></i></td>
      <td>Add User</td>
      <td>{{newUser.firstName}}{{if newUser.lastName newUser.lastName}} ({{newUser.name}})</td>
    </tr>
    {{#if newDomain}}
      <tr class="{{if operations.domain 'success'}}">
        <td><i class="fa fa-fw {{if operations.domain 'fa-check' 'fa-spinner'}} {{if operations.domain 'text-success' 'fa-spin'}}"></i></td>
        <td>Assign Domain</td>
        <td>{{newDomain}}</td>
      </tr>
    {{/if}}
    <tr class="{{if operations.timezone 'success'}}">
      <td><i class="fa fa-fw {{if operations.timezone 'fa-check' 'fa-spinner'}} {{if operations.timezone 'text-success' 'fa-spin'}}"></i></td>
      <td>Set Timezone</td>
      <td>{{tzRegion}} ({{tzZone}})</td>
    </tr>
    {{#if resizeSDCard}}
      <tr class="{{if operations.resizeSDCard 'success'}}">
        <td><i class="fa fa-fw {{if operations.resizeSDCard 'fa-check' 'fa-spinner'}} {{if operations.resizeSDCard 'text-success' 'fa-spin'}}"></i></td>
        <td>Resize SD Card</td>
        <td>True</td>
      </tr>
    {{/if}}
    {{#if useGPUMem}}
      <tr class="{{if operations.useGPUMem 'success'}}">
        <td><i class="fa fa-fw {{if operations.useGPUMem 'fa-check' 'fa-spinner'}} {{if operations.useGPUMem 'text-success' 'fa-spin'}}"></i></td>
        <td>Reassign GPU Memory</td>
        <td>True</td>
      </tr>
    {{/if}}
    {{#if cubieMAC}}
      <tr class="{{if operations.cubieMAC 'success'}}">
        <td><i class="fa fa-fw {{if operations.cubieMAC 'fa-check' 'fa-spinner'}} {{if operations.cubieMAC 'text-success' 'fa-spin'}}"></i></td>
        <td>Set MAC Address</td>
        <td>{{cubieMAC}}</td>
      </tr>
    {{/if}}
    {{#each model.apps as |app|}}
      {{#if app.firstrunSelected}}
        <tr class="{{if app.installed 'success'}}">
          <td><i class="fa fa-fw {{if app.installed 'fa-check' 'fa-spinner'}} {{if app.installed 'text-success' 'fa-spin'}}"></i></td>
          <td>Install</td>
          <td><i class="fa-fw {{app.icon}}"></i> {{app.name}}</td>
        </tr>
      {{/if}}
    {{/each}}
  </table>
  {{#if canFinish}}
    <div class="text-center">
      <p>You may begin to use arkOS while your applications install in the background.</p>
      <a href="#" class="btn btn-primary btn-lg" {{action "finish"}}><i class="fa fa-check"></i> Finish</a>
      {{#if needsRestart}}
        <p>Some of the options you chose require a restart to the device before they will take effect. Please wait until your applications have installed and show up before attempting to restart.</p>
      {{/if}}
    </div>
  {{/if}}
</div>

<xml>
    <label size="5" text="Calendar/Contacts Server Setup" />
    <pad id="main">
    	<simpleform id="frmSetup">
    	    <formline text="Address" help="This should be the web address you want this calendar server to be visible on. For example, 'www.example.com'. It must be a DNS-resolvable name pointing at this server.">
    	        <textinput name="addr" id="addr" />
    	    </formline>
    	    <formline text="Port" help="If you don't know what to put here, leave it at default (port 80).">
    	        <textinput name="port" id="port" value="80" />
    	    </formline>
    	    <formline>
    	        <button onclick="form" form="frmSetup" design="primary" action="OK" text="Setup" />
    	    </formline>
    	</simpleform>
    </pad>
</xml>

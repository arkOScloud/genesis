import cardColor from '../../../utils/card-color';
import { module, test } from 'qunit';

module('Unit | Utility | card color');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = cardColor();
  assert.ok(result);
});

import { cardColor } from '../../../helpers/card-color';
import { module, test } from 'qunit';

module('Unit | Helper | card color');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = cardColor(42);
  assert.ok(result);
});
